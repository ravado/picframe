########################################
# 📂 SMB Server Configuration
########################################

# Base SMB server address (no leading //)
SMB_HOST="192.168.91.198"

# Backup share name and subdir
SMB_BACKUPS_SHARE="Backups"
SMB_BACKUPS_SUBDIR="PhotoFrames"

# PicFrame share name and subdir
SMB_PICFRAMES_SHARE="Photo-Frames"
SMB_PICFRAMES_SUBDIR="Home"

# Derived full SMB paths (double slash format)
SMB_BACKUPS_PATH="//${SMB_HOST}/${SMB_BACKUPS_SHARE}"
SMB_PICFRAMES_PATH="//${SMB_HOST}/${SMB_PICFRAMES_SHARE}"

########################################
# 🔑 SMB Credentials
########################################
# Local system user that runs picframe (owns /home/$PICFRAME_USER)
PICFRAME_USER="{picframe_user}"

# Samba server user credentials (for server-side restore)
USERNAME="{username}"
PASSWORD="{password}"

# SMB client credentials (for mounting from this machine)
SMB_CRED_USER="{username}"
SMB_CRED_PASS="{password}"

########################################
# 🔄 Remote Sync Settings
########################################
# Remote user and host to sync photos from another PicFrame
REMOTE_USER="{remote_user}"
REMOTE_HOST="10.0.0.103"

# Paths for photo synchronization
REMOTE_PATH="/home/ivan.cherednychok/Pictures/"
LOCAL_PATH="/home/ivan/Pictures/PhotoFrame/"